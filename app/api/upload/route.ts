import {admin,bucket,sameOrigin} from '@/lib/content';
export async function POST(req:Request){if(!sameOrigin(req)||!await admin())return Response.json({error:'Unauthorized'},{status:403});try{const form=await req.formData();const file=form.get('file');if(!(file instanceof File)||file.size>30*1024*1024)return Response.json({error:'Maximum file size: 30 MB'},{status:400});if(!file.type.startsWith('image/')&&!file.type.startsWith('video/'))return Response.json({error:'Use Image or Video'},{status:400});const id=crypto.randomUUID();await bucket().put(id,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});return Response.json({url:'/api/media/'+id,type:file.type.startsWith('video')?'video':'image'});}catch (e) {console.error(e); return Response.json({error:'Upload failed. Please retry.'},{status:503})}}
