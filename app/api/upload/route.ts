import {admin,bucket,sameOrigin} from '@/lib/content';
export async function POST(req:Request){if(!sameOrigin(req)||!await admin())return Response.json({error:'Unauthorized'},{status:403});try{const form=await req.formData();const file=form.get('file');if(!(file instanceof File)||file.size>30*1024*1024)return Response.json({error:'Maximum file size: 30 MB'},{status:400});const allowed=['image/jpeg','image/png','image/webp','image/gif','video/mp4','video/webm'];if(!allowed.includes(file.type))return Response.json({error:'Use JPG, PNG, WebP, GIF, MP4 or WebM'},{status:400});const id=crypto.randomUUID();await bucket().put(id,file.stream(),{httpMetadata:{contentType:file.type}});return Response.json({url:'/api/media/'+id,type:file.type.startsWith('video')?'video':'image'});}catch{return Response.json({error:'Upload failed. Please retry.'},{status:503})}}
